# delivery-bot
# El robot envia el valor del reloj a los motores
# El reloj varia entre 0 y 256 (decimas de segundo)

#Input
INPUT_CLOCK = 0
#Outputs
OUTPUT_ENGINE_L = 1
OUTPUT_ENGINE_R = 2

do {
  clock <- read INPUT_CLOCK
  output OUTPUT_ENGINE_L clock
  output OUTPUT_ENGINE_R clock
}
