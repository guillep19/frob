
Lenguaje.

 - Hacer dsl en Haskell

Haskell genera codigo de bajo nivel.

 - Codigo se interpreta en la VM.


Threads en la VM:

Cada Task se comporta como un thread.

El task puede estar en varios estados:
 - Deshabilitado
 - Corriendo
 -
 
Ejemplo de programa:
--------------------

Tipos basicos:
  Bool: True | False
  Int: x in { -1

Input Color color-izquierdo
Input Color color-derecho
Input Distance distancia-casa
Output Motor motor-izquierdo
Output Motor motor-derecho

var EsBlanco = function(Color c):
    return c == 0

//setVelocidad :: Motor x Velocidad -> Motor

type Motor {
  float setVelocidad(float velocidad)
}

type ColorSensor {
  colors: [WHITE, BLACK],
  get_color: function() {
    
  }
}

