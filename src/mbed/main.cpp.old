#include "mbed.h"

DigitalOut myled(LED1);

int main()
{
    while(1) {
        myled = 1;
        wait(5);
        myled = 0;
        wait(5);
        //int* a = new int[10000];
    }
}
